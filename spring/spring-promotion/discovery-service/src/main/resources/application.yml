server:
  port: 8761

spring:
  application:
    name: discovery-service



eureka:
  client:
    register-with-eureka: false # discovery 자체 서버 이기에 false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0  # 해당 시간후 재시도 (개발중 이기에 바로 바로 확인 위해)